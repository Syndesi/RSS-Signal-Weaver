[project]
name = "signalweaver"
version = "0.1.0"
requires-python = ">=3.13"

dependencies = [
    "prefect>=2.16",
    "feedparser",
    "langdetect",
    "structlog",
    "marimo",
    "numpy",
    "pandas",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "pip>=26.0.1",
    "pytest",
    "ruff",
    "ty"
]
