[project]
name = "signalweaver"
version = "0.1.0"
requires-python = ">=3.13"

dependencies = [
    "prefect>=2.16",
    "feedparser",
    "langdetect",
    "structlog",
    "marimo",
    "numpy",
    "pandas",
]

dependency-groups.dev = [
    "pytest",
    "ruff",
    "ty"
]
